# Captioning File (Roof Report Image Extraction)

(Alias of roof_report_captioning.md). See that file for full specification.

Purpose: Provide Gemini with structured, role-oriented instructions to extract:
1. Length measurements (categorized + totals)
2. Facet areas (individual + total, largest/smallest)
3. Pitch values (grouped + extremes)
4. Azimuth values with cardinal directions (orientation of largest facets)
5. Aerial / elevation observations: penetrations, obstructions, condition

Return ONLY JSON as described in `roof_report_captioning.md`.
